language: ruby
cache: bundler
bundler_args: --deployment

rvm:
  - 2.0.0

env:
  matrix:
    - TEST_SUITE=rubocop
    - TEST_SUITE=spec
    - TEST_SUITE=spec_js
    - TEST_SUITE=cucumber

script: bundle exec rake $TEST_SUITE
